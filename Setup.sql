USE gregslistmysql;

-- CREATE TABLE cars
-- (
--     id INT AUTO_INCREMENT,
--     make VARCHAR(255) NOT NULL UNIQUE,
--     model VARCHAR(255) NOT NULL UNIQUE,
--     year INT NOT NULL UNIQUE,
--     miles INT NOT NULL UNIQUE,
--     price INT NOT NULL UNIQUE,

--     PRIMARY KEY (id)
-- )

-- CREATE TABLE jobs
-- (
--     id INT AUTO_INCREMENT,
--     jobTitle VARCHAR(255) NOT NULL UNIQUE,
--     company VARCHAR(255) NOT NULL UNIQUE,
--     description VARCHAR(255) NOT NULL UNIQUE,
--     rate INT NOT NULL UNIQUE,
--     hours INT NOT NULL UNIQUE,

--     PRIMARY KEY (id)
-- )

-- CREATE TABLE houses
-- (
--     id INT AUTO_INCREMENT,
--     description VARCHAR(255) NOT NULL UNIQUE,
--     bedrooms INT NOT NULL UNIQUE,
--     bathrooms INT NOT NULL UNIQUE,
--     price INT NOT NULL UNIQUE,
--     levels INT NOT NULL UNIQUE,

--     PRIMARY KEY (id)
-- )